name: "AI PR Reviewer"
description: "Posts an automated review comment on pull requests"
author: "you"

inputs:
  message:
    description: "Message to post as PR comment"
    required: true

runs:
  using: "node20"
  main: "index.js"